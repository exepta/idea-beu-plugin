plugins {
    id 'java'
    id 'org.jetbrains.intellij.platform'
}

group = 'com.tiltus.beu.parser'
version = findProperty('pluginVersion') ?: '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

dependencies {
    intellijPlatform {
        rustRover('2026.1')
    }
}

intellijPlatform {
    pluginConfiguration {
        name = 'Bevy HTML Support'

        ideaVersion {
            sinceBuild = '261'
        }
    }

    pluginVerification {
        ides {
            // TODO: switch this back to RustRover once Plugin Verifier supports current RR layout.
            create('IU', '2026.1')
        }
    }
}
